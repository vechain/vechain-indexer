name: Test Package

on:
  workflow_call:
    inputs:
      package:
        required: true
        type: string
      runsOn:
        required: true
        type: string

permissions:
  contents: read
  id-token: write
  packages: write
  checks: write

jobs:
  test-gradle-package:
    runs-on: ${{ inputs.runsOn }}
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          distribution: 'oracle'
          java-version: '17'

      - name: Gradle Test
        run: ./gradlew :package:${{ inputs.package }}:cleanTest :package:${{ inputs.package }}:test

      - name: Debug E2E
        if: failure() && inputs.package == 'e2e'
        run: |
          export INDEXER_ENV_FILE_NAME=packages/indexer/.env.example
          export API_ENV_FILE_NAME=packages/api/.env.example
          docker network ls
          docker ps
          docker ps -a
          docker network inspect vechain-database || true
          docker network inspect vechain-thor || true
          docker network inspect vechain || true
          docker compose -f database/docker-compose-mongo.yaml logs
          docker compose -f database/docker-compose-mongo-setup.yaml logs
          docker compose -f thor/docker-compose.yaml logs
          docker compose logs

      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v3
        if: success() || failure()
        with:
          report_paths: packages/${{ inputs.package }}/build/test-results/test/TEST-*.xml

      - name: Copy HTML Test Report
        if: github.ref == 'refs/heads/main'
        run: |
          mkdir -p static/${{ inputs.package }}
          if [ -d "packages/${{ inputs.package }}/build/reports/jacoco/test/html" ]; then
            cp -r packages/${{ inputs.package }}/build/reports/jacoco/test/html static/${{ inputs.package }}/coverage
            cp -r packages/${{ inputs.package }}/build/reports/tests/test static/${{ inputs.package }}/tests
          fi

      - name: Upload Test Results
        uses: actions/upload-artifact@v2
        if: github.ref == 'refs/heads/main'
        with:
          name: test-results-${{ github.head_ref || github.ref_name }}
          retention-days: 1
          path: static