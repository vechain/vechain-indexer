name: 'Security Checks'
on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  scorecard:
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    uses: vechain/github-actions/.github/workflows/scorecard.yaml@72c47d9d8d3f9ed4404f9056f48967e524b43ae9 # v.1.13.0
    secrets:
      app-id: ${{ secrets.SCORECARD_APPID }}
      private-key: ${{ secrets.SCORECARD_PRIVATEKEY }}

  checkov:
    uses: vechain/github-actions/.github/workflows/checkov.yaml@72c47d9d8d3f9ed4404f9056f48967e524b43ae9 # v.1.13.0

  infracost:
    uses: vechain/github-actions/.github/workflows/infracost.yaml@72c47d9d8d3f9ed4404f9056f48967e524b43ae9 # v.1.13.0
    secrets:
      INFRACOST_API_KEY: ${{ secrets.INFRACOST_API_KEY }}
      GIT_VECHAINCI_SSH_KEY: ${{ secrets.VECHAINCI_SSH_PRIVATE_KEYS }}
