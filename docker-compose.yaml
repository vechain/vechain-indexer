version: "3.5"

services:

  vechain-indexer:
    build:
      context: .
      args:
        VEWORLD_PACKAGE: indexer
    container_name: vechain-indexer
    restart: "no"
    env_file: .env.local
    networks:
      - vechain
      - vechain-database
      - vechain-thor

  vechain-indexer-api:
    build:
      context: .
      args:
        VEWORLD_PACKAGE: api
    container_name: vechain-indexer-api
    restart: "no"
    env_file: .env.local
    ports:
      - 8080:8080
    networks:
      - vechain
      - vechain-database
      - vechain-thor

networks:
  vechain:
    driver: bridge
    name: vechain
  vechain-database:
    external: true
    name: vechain-database
  vechain-thor:
    external: true
    name: vechain-thor
